[project]
name = "crypto-tracker"
version = "0.1.0"
description = "Live crypto price tracker with GPU-accelerated charts"
requires-python = ">=3.14"
dependencies = [
    "dearpygui>=2.3.1",
    "requests>=2.33.1",
]

[tool.mypy]
ignore_missing_imports = true
